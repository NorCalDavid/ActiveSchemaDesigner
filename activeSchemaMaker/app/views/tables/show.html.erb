<div id ="t<%= @table.id %>" class="tbl-frame debug">

  <div class="title">
    <span><%= @table.name %></span>
    <button id="tb<%= @table.id %>" class="edit" title="tbl-edit">Edit</button>
  <div>

<% unless @table.fields == [] %>
  <table class="fields">
    <% @table.fields.each do |field| %>
      <tr class="field-cell">
        <td id="field.id"><%= field.name %></td>
      </tr>
    <%end%>
  </table>
<% end %>
