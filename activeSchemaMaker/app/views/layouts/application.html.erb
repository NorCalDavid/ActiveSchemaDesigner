<!DOCTYPE html>
<html>
<head>
  <title>ActiveSchemaMaker</title>
  <link rel="stylesheet" type="text/css" href="http://fonts.googleapis.com/css?family=Open+Sans">
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>
</head>
<body data-project-id="<%= @project.try(:id) %>">

<div class="header home-menubar-fixed">

  <div class="home-menu pure-menu pure-menu-horizontal pure-menu-fixed blue">
    <div class="pull-right">
      <ul class="pure-menu-list">
        <% if current_user %>
        <%=link_to "Active Schema Designer", user_path(current_user.id), :class => "pure-menu-heading"%>
          <li class="pure-menu-item"><%= link_to "#{current_user.first_name}", user_path(current_user.id), :class => "pure-menu-link" %></li>
          <li class="pure-menu-item"><%= link_to "Logout", '/logout', :class => "pure-menu-link" %></li>

        <% else %>
          <li class="pure-menu-item"><%= link_to 'Login', '/login', :class => "pure-menu-link" %></li>
          <li class="pure-menu-item"><%= link_to 'Signup', '/signup', :class => "pure-menu-link" %></li>
        <% end %>

      </ul>
    </div>
  </div>

</div>

<div class="content pure-u-1">

  <%= yield %>

</div>

</body>
</html>
